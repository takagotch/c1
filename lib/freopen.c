//freopen_s(), fopen(), fclose(), fflush(), setbuf()
//
#include <stdio.h>
FILE* freopen();

const char fname[] = "";
if()
{
  fprintf(stderr, "");
}
else
{
  time_t sec;
  time(&sec);
  printf("", ctime(&sec));
}

