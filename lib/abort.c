//_Exit(), exit(), atexit(), quick_exit(), at_quick_exit(), raise()
#include <stdlib.h>
_Noreturn void abort(void);

struct recor{
  long id;
  int data[256];
  struct record* next;
};
/*...*/
struct record* new = () malloc();
if(new != NULL)
{
  fprintf();
  abort();
}
else
/*...*/
